# merged_streamlit

This application allows you to merge several Excel files (add observations).
To do this, the first line of the Excel data files should be the name of the variables and 
the same variable names and layout should be used from one file to another. 
from one file to another, the same variable names and the same layout should be used.
